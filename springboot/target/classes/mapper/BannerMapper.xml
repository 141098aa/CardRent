<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.BannerMapper">

    <resultMap id="BaseResultMap" type="com.example.entity.content.Banner">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="image" column="image"/>
        <result property="keyword" column="keyword"/>
        <result property="link" column="link"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        select * from banner
        <where>
            <if test="keyword != null and keyword != ''">
                and (title like concat('%', #{keyword}, '%')
                or description like concat('%', #{keyword}, '%'))
            </if>
        </where>
        order by sort_order asc, create_time desc
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        select * from banner where id = #{id}
    </select>
    <select id="selectEnabled" resultMap="BaseResultMap">
        select * from banner where status = 1 order by sort_order asc, create_time desc
    </select>

    <insert id="insert" parameterType="com.example.entity.content.Banner" useGeneratedKeys="true" keyProperty="id">
        insert into banner (title, description, image, keyword, link, sort_order, status, create_time)
        values (#{title}, #{description}, #{image}, #{keyword}, #{link}, #{sortOrder}, #{status}, now())
    </insert>

    <update id="update" parameterType="com.example.entity.content.Banner">
        update banner
        set title = #{title},
            description = #{description},
            image = #{image},
            keyword = #{keyword},
            link = #{link},
            sort_order = #{sortOrder},
            status = #{status},
            update_time = now()
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from banner where id = #{id}
    </delete>

    <delete id="batchDelete">
        delete from banner where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="updateStatus">
        update banner set status = #{status} where id = #{id}
    </update>

</mapper>